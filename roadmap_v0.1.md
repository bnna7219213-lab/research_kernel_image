# 自研文生图（T2Image）路线图 v0.1

> 版本：v0.1（2026-09-22）
> 输入：plan.md 技术规划 + 共享 8×A100-80GB K8s 池（与 research_kernel_music 分时）
> 目标：PoC 阶段产出 512×512 text-to-image 首版，可商用 + 可控微调。

---

## 总体路线

```
 ┌────────────────────────────────────────────────────────────────────┐
 │ M1 (W1–W4)   复现开源基线 + 数据管线 + 环境固化                │
 │ M2 (W5–W12)  短图 PoC 渲染器训练 512px，跑通 eval 体系        │
 │ M3 (W13–W18) DMD 2-step 蒸馏 + 推理加速                       │
 │ M4 (W19–W26) 垂类微调 + 首版评审 + 下一阶段立项目标            │
 └────────────────────────────────────────────────────────────────────┘
```

### 核心原则
1. **复用开源框架**：PixArt-Σ + Flux 训练栈（MIT / Apache），不重造轮子
2. **重点投入在数据与条件注入**：通用基础模型不强求 SOTA，垂类可控性与调优速度才是竞争力
3. **与音乐项目分时共享 8 卡池**：默认 50% 配额，高峰期 2 周轮换一次，CI 化排队
4. **评估双轨并行**：自动指标（FID/CLIP/PickScore）+ 业务人工盲听，每期都出对比报告

---

## M1 (W1–W4)：基线复现与工程准备

### 任务分解

| 编号 | 任务 | 交付物 | 负责 |
|---|---|---|---|
| W1.1 | 构建 Docker（PyTorch 2.5 + CUDA 12.4 + 评估栈） | `docker/` | 工程 |
| W1.2 | 跑通 PixArt-Σ + Flux.1-dev 推理（8 卡 PV 调度） | ≥40 条 demo 图 | 工程 |
| W1.3 | 编写数据清洗与 recaption 脚本 | 自动化管线 | 数据 |
| W1.4 | 编写数据指纹工具 + VAE 评估 baseline | FID-Patch 基线 | 工程 |
| W1.5 | K8s Job 模板（与音乐池共享，分时策略） | `k8s/` | 工程/运维 |

### M1 验收标准
- [ ] 至少 2 个开源基线（PixArt-Σ + Flux.1-dev）推理跑通
- [ ] 数据清洗管线可跑通（自动 NSFW、CLIP score 过滤）
- [ ] 自动评估脚本（FID-10K）可用
- [ ] K8s 8 卡任务提交并获调度

---

## M2 (W5–W12)：短图 PoC 渲染器训练

### 子任务
- **Stage A**（W5–W8）：VAE/DCAE 自训练 + codec 评估，确定最终 VAE 路线
- **Stage B**（W7–W12）：DiT 渲染器（3B）预训练，512px + Flow Matching
- **持续**：W&B 记录每 5000 step 出 eval 报告

### M2 验收标准
- [ ] VAE 重建 FID ≤ 开源 SD-VAE 同条件下 1.1×
- [ ] 3B DiT 渲染器独立产出 512px 可用图，FID ≤ 18 / CLIP ≥ 0.32
- [ ] 内部盲测 MOS ≥ 3.5 / 5（10 人 × 50 prompts）
- [ ] W&B 实验记录完整

---

## M3 (W13–W18)：蒸馏与加速

### 子任务
- DMD 2-step 蒸馏（M2 最终 ckpt 起步）
- DeepCache + torch.compile 推理加速
- LoRA 微调入口 + 基础 ControlNet 插件

### M3 验收标准
- [ ] 2-step 推理速度 ≤ 4s / 张（A100-80GB）
- [ ] 蒸馏后 FID 退化 ≤ +1.0（相对 M2 原版）
- [ ] LoRA 单垂类微调 ≤ 1 小时（8 卡）

---

## M4 (W19–W26)：垂类微调与首版评审

### 子任务
- 选定 2–3 个业务垂类（电商图、海报、IP 形象），LoRA 微调
- 与人类评审团双周对齐
- 产出产品化 API 网关 demo

### M4 验收标准
- [ ] 垂类 FID ≤ 12 / PickScore ≥ 21
- [ ] API P95 ≤ 8s（512px, 2-step）
- [ ] 评审纪要 + 下一阶段立项数据

---

## 共享算力调度策略

```
时间轴 (周)
|-- W1–W8 --|-- W9–W16 --|-- W17–W24 --|
   音乐: 75%     音乐: 50%     文生图: 75%
   文生图:25%    文生图:50%     音乐:  25%
```
> M1/M3 文生图任务较轻；M2 渲染训练高峰期优先保障 4 卡以上。

### K8s 分时 Job 模板
- HPA + priorityClass：音乐任务 class=high，文生图 class=normal
- 每个项目预留"低优先级槽位"：空闲显卡可抢占式使用

---

## 风险与应对 — T2Image 视角

| 风险 | 触发条件 | 应对 |
|---|---|---|
| 算力被音乐抢占 | 音乐 M2/M3 同期高峰 | 保留 4 卡"兜底配额"，最坏情况下跑 4 卡 |
| LAION 数据下载失败 | 区域网络限制 | 使用 HF-mirror、ModelScope 镜像；国内中转 |
| 评估与业务脱节 | FID 好看但业务侧不满意 | 每 4 周一次人工评审，前 6 周就拉业务方介入 |
| 中文解析弱 | prompt 含复杂中文场景 | text encoder 加双语对齐微调（LoRA） |
| 开源模型快速超越 | 新版本 Flux/SD 跃升 | 不追 SOTA，保留垂类定制与微调速度即可 |
